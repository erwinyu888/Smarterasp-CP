# Rebuild Blockers

Record features that cannot be completed because an exact referenced Classic ASP dependency is missing, a live gateway is unavailable, or a real-data test cannot be completed safely.

## Working Rule

- Use the exact file referenced by the Classic ASP feature being rebuilt.
- Ignore code inside `if 1 = 2 then ... end if` blocks because the latest ASP uses that pattern for intentionally disabled legacy behavior.
- If that referenced file is missing, stop that feature and log it here.
- Do not use similar files from other folders or older copies unless the exact latest source chain references them.
- Use `workqueue` only when the exact Classic ASP source for that feature writes to `workqueue` or explicitly launches a background worker. Direct IIS/API/DB actions must not be converted into queued jobs.
- Continue with the next independent feature after logging the blocker.

## Current Blockers

| Date | Area | Feature | Missing/Blocked Dependency | Evidence | Needed From User | Status |
|---|---|---|---|---|---|---|
| 2026-06-12 | Account Billing | Account-to-account balance transfer | Skipped because it is not exposed by the active customer-facing `.asp` account pages | The only latest-source hits found were SOAP test/helper clients such as `/Users/erwinyu/Downloads/hosting/CP/WebServices/omsAdmin/WebServiceClients/TransferCredit.asp`; `/Users/erwinyu/Downloads/hosting/cp8/account/accountbalance.asp` exposes deposit/top-up, not customer balance transfer. Per rebuild rule, helper-only flows are not included in the new panel. | Provide the exact active customer-facing `.asp` page that exposes balance transfer if this should be rebuilt. | Skipped |
| 2026-06-12 | Account Settings | Enable 2FA setup | Needs exact legacy `encryptpwd`/`decryptpwd` compatibility before storing or reading TOTP secrets | Latest `/Users/erwinyu/Downloads/hosting/cp8/account/2fa_action.asp` creates a secret via `http://member5.smarterasp.net/2fa/ga_create.php`, stores it encrypted in `[ehbconfig].[dbo].[2fa]`, decrypts it for QR generation, and verifies via `ga_verify.php`. The rebuilt app reads and disables existing 2FA rows, but enabling new 2FA must not store secrets with a non-compatible encryption routine. | Provide known plaintext/encrypted samples for `encryptpwd`/`decryptpwd`, or approve a new rebuilt-only 2FA secret format with a migration boundary. | Blocked |
| 2026-06-12 | Account Settings | Account re-verification email | Needs exact legacy `encryption(importkey2, now\|customerid)` compatibility and the production email template before sending real verification links | Latest `/Users/erwinyu/Downloads/hosting/cp8/account/re-verify_action.asp` reads `C:\hosting\AP\marketing_admin\emailtemplates\re-verify.html`, replaces `[GUID]` with a legacy-encrypted timestamp/customer id token, and sends through `SendHTMLEmail`. The rebuilt panel shows verification status and supports email-change verification records, but should not send incompatible account verification links. | Provide the exact template and a compatible `importkey2` encryption/decryption sample or approve a rebuilt-only verification token path. | Blocked |
| 2026-06-12 | Account Settings | SMS phone verification | Needs SMS gateway config and disposable phone test target before live send/update | Latest `/Users/erwinyu/Downloads/hosting/cp8/account/profile_update_sms.asp` sends a PIN through `www.smarterasp.net/sms/sms2.php`, writes `[ehbconfig].[dbo].[sms_verify]`, rate-limits to 3 sends per 10 hours, blocks country code 7, rejects duplicate mobile numbers, and updates `customer_profile.mobile_number` after PIN confirmation. The rebuilt profile editor updates mobile directly today; the full PIN flow should replace that once gateway settings and a test phone are confirmed. | Provide/confirm SMS gateway policy for dev tests and a disposable phone number. | Blocked |
| 2026-06-12 | Account Domain Transfer | OpenSRS transferability check before checkout | Exact XML template referenced by the latest ASP is missing | `/Users/erwinyu/Downloads/hosting/cp8/account/transfer_domain_step1.asp` calls `readfile(server.mappath("./xmlsample/checktransfer.txt"))` inside `CheckDomainTransfer(domain)`, but `checktransfer.txt` is not present anywhere under `/Users/erwinyu/Downloads/hosting` outside ignored `nouse` paths. The rebuild now includes the exact `CheckDomainTransferInDB` duplicate-domain guard from `functions.inc`, but cannot safely reproduce the OpenSRS transferability call without the exact XML payload. | Provide `/account/xmlsample/checktransfer.txt` or the exact active replacement file used by production. | Blocked |
| 2026-06-12 | Websites | Live IIS config reads for Default Doc, Custom Errors, MIME, ScriptMap | WIN8146 IIS agent rejected read-only `IIS_api.asp` calls with HTML/401-style response | The rebuilt drawers now call exact classic read actions from `/Users/erwinyu/Downloads/hosting/cp8/functions/fn_site.inc`: `IIS_Member_DefaultPage_Get`, `IIS_Member_ErrorPage_Get`, `get_mimeMap`, and `IIS_Member_IISEntry_CustomScriptMap_Get`. A read-only smoke test against `win8146.site4now.net:830/IIS_api.asp` returned an HTML rejection, so no IIS state was changed. | Confirm port 830/IIS_api access for the ASP.NET host or provide the required internal host/header/auth path for WIN8146. | Blocked |
| 2026-06-12 | Websites | Domain Manager add/remove/move writes | Exact side-effect chain is broader than IIS binding and must be ported before enabling writes | Latest `/Users/erwinyu/Downloads/hosting/cp8/cp/domainbind_actions.asp` add/remove paths call IIS binding helpers, DNS zone build/delete helpers, domain quota/trial-domain checks, first-domain updates, free SSL cleanup, and hosted/corporate mail deletion. Enabling only `IIS_Member_Binding_Domain_Add/Delete` plus DB changes would leave DNS/mail/SSL state inconsistent, so writes remain blocked until the full chain is ported. | Confirm DNS gateway access and disposable customer domain for add/remove/move tests; provide any missing exact include referenced by DNS/mail helper chain if the current latest source is incomplete. | Blocked |
| 2026-06-12 | FTP | FTP create/password update | Need exact Persits-compatible `encryptpwd` and `encryptFTPpwd` output before writing FTP credentials | Latest `/Users/erwinyu/Downloads/hosting/cp8/functions/functions.inc` active `createFTPSingle(...)` and `updateFtpUserPassword(...)` write DB rows directly. The `/ftp_api.asp` calls are inside disabled `if 1 = 2 then ... end if` blocks and are ignored. Encryption functions are in `/Users/erwinyu/Downloads/hosting/includes/encryption.asp`; constants were found in `/Users/erwinyu/Downloads/hosting/api/encrypt.asp`, but the .NET implementation must match Persits `CryptoManager.EncryptText(...).hex` exactly before real FTP rows can be created safely. | Provide known plaintext/encrypted sample pairs for `encryptpwd` and `encryptFTPpwd`, or provide a supported way to run the same Persits encryption from the new app. | Blocked |
| 2026-06-12 | Websites | Real testing Site On/Off and other IIS actions | Need a disposable new website plus reachable legacy IIS agent before running start/stop/delete-style tests | Existing Site On/Off implementation maps to exact `fn_site.inc` actions `IIS_Member_IISEntry_START` / `IIS_Member_IISEntry_STOP`, but testing against existing sites would touch user data. New site provisioning depends on the same remote-agent path that currently timed out for FTP. | Restore/confirm legacy agent reachability, then create a disposable `codex-test-*` site and run IIS start/stop/delete tests only against that new site. | Blocked |
| 2026-06-12 | Email | Real SmarterMail write tests on existing domains | Need a disposable email domain before running password/quota/delete writes | Email-domain inventory loads real `cp_config_EmailDomains` data for `openreward`. The rebuilt app now uses the active latest Classic ASP `mail_api` default from `/Users/erwinyu/Downloads/hosting/includes/configs_individual.inc` (`http://member.smarterasp.net/mailboxquota2`) and can call `/setpassword.aspx`, `/setdomainquota.aspx`, and `/deletedomain.aspx`. Existing domains must not be mutated for tests. | Create/confirm a disposable email domain for live password/quota/delete tests. | Blocked |
| 2026-06-12 | Email | Daily send limit, DKIM generation, and mailbox-level actions | Need exact latest SmarterMail SOAP/templates and masssmtp purchase/action chain ported before write | Latest `/Users/erwinyu/Downloads/hosting/cp8/cp/email_action.asp` includes `deletemasssmtp` and links to `/account/addon_purchase_dailysentlimit_active`; latest `/Users/erwinyu/Downloads/hosting/cp8/functions/smartermailapi.inc` contains SOAP helpers for forwarding/password/user info. The rebuilt UI now routes DNS/Webmail to real read-only data, and hosted-domain password/quota/delete use the SmarterMail gateway, but mailbox/DKIM/daily-send-limit write flows need their exact SOAP request templates and product-purchase side effects mapped. | Confirm disposable mailbox/domain targets and whether SmarterMail SOAP auth from `smartermailapi.inc` should be moved into `.env` for live dev writes. | Blocked |
| 2026-06-12 | Websites | Scheduled Task inventory/create/delete | Missing `scheduletask` connection string | Latest ASP `cp/task.asp`, `cp/task1.asp`, `cp/task_action.asp`, and `cp/taskremove.asp` use the `tasks` table through the Classic ASP `scheduletask` connection. Focused searches found the SQL shape and task commands, but no `scheduletask` connection string in this project `.env`/`appsettings` or the currently supplied latest source files. | Provide the exact `scheduletask` connection string or the latest include file that defines it, then move it into `.env` and port inventory/create/delete. | Blocked |
| 2026-06-12 | CDN | Live Cloudflare enable/disable | Shared gateway rejected the rebuilt command with an HTML response | Latest ASP maps CDN actions through `/Users/erwinyu/Downloads/hosting/cp8/functions/functions.inc:Run_cloudflare_cmd`, posting `cmdstr` to `http://api.smarterasp.net:80/api/cloudflare_api.asp`. A real request for temporary domain `openreward-001-site1.atempurl.com` reached that endpoint but was rejected with an HTML response. The rebuilt endpoint now blocks temp URLs and requires a mapped customer domain. | Provide/confirm a disposable customer domain mapped to the test hosting account, and confirm whether `api.smarterasp.net` requires internal network headers/auth beyond the old form post. | Blocked |
| 2026-06-12 | Websites | Outgoing Port add/delete live firewall write | Shared firewall gateway rejected rebuilt request with an HTML response | Latest ASP `/Users/erwinyu/Downloads/hosting/cp8/cp/sqlremoteip_action.asp` calls `set_Firewall_rpc`, which posts `action`, `ServerID`, `IP`, `port`, and `rulename` to `http://api.smarterasp.net:80/api/firewall_api.asp`, then inserts/deletes `dbo.cp_config_db_out_ip`. The rebuild now loads existing outgoing-port rows and has add/delete mutations, but a live disposable add for `1.1.1.1:443` was rejected by the shared gateway before any DB row was inserted. | Confirm whether `api.smarterasp.net` requires internal network headers/auth, a different source IP, or a different host for rebuilt calls. | Blocked |
| 2026-06-12 | Websites | WebDeploy / Remote IIS enable-disable and publish profile | Need Persits-compatible `decryptpwd` for `cpPasswordHash` before calling `SetIISManagerUser` or generating credentials | Latest ASP `/Users/erwinyu/Downloads/hosting/cp8/cp/iis_manager_webdeploy_action.asp` decrypts `getCpPasswordHashByCpID(session("cpID"))`, hashes it with SHA-256, calls `/IIS_api.asp?action=SetIISManagerUser`, then calls `AddIISManagerUser`/`DelIISManagerUser`. The rebuild now reads status with `/IIS_api.asp?action=CheckIISManagerUser`, shows connection info, and can submit Fix ACL through `/tools/resetDefaultPermissionsBySiteUid.asp`, but enable/disable and publish settings must not be guessed because they expose/use the real hosting password. | Provide known plaintext/encrypted samples for `decryptpwd`, or provide a compatible legacy decrypt service, then move its key/config into `.env`. | Blocked |
| 2026-06-12 | Files | Live file-manager write through JSON `/new/fileManagerAction.asp` | None for browse/create/read/save/rename/delete/upload/download with disposable data | ASP.NET now encrypts `u`/`b`/`p` for both browse and action agents with `FILE_MANAGER_ENCRYPT_KEY`. Live test against `cpId=10373089` loaded `H:\root\home\openreward-001\www`, created disposable folder `codex-test-*`, verified it appeared in browse search, deleted it, and verified it no longer appeared. Disposable `codex-test-file-*.txt` files were created, read, saved, renamed, downloaded through the browser read path, uploaded through the browser save path, and deleted through the real `/new/fileManagerAction.asp`. Path-abuse checks for `h:\root\home`, `..\`, and `\..\` returned `400` before reaching the agent. | Continue adding additional file actions such as move/copy/zip/unzip with disposable `codex-test-*` data only. | Unblocked |
| 2026-06-12 | Files | Unzip through encrypted JSON action agent | Need a disposable real `.zip` file inside the customer-owned folder before live unzip test | ASP.NET now normalizes `/www` as the hosting root alias, forwards encrypted `targetPath`, and forwards optional `targetName` for copy/move. Live `openreward-001` tests on WIN8146 created disposable `codex-test-*` folders/files, copied a file between disposable folders, moved it back, deleted all disposable artifacts, and confirmed path-abuse requests for `h:\root\home`, `..\`, `/www/../`, and `/www/../../` return `400` before reaching the agent. Local `legacy-agents/fileManagerAction.asp` also supports optional rename-on-copy/move for the next upload. Unzip is still untested because no disposable valid zip file was present and the existing `zip` path queues work rather than creating an immediate test artifact. | Provide or create a disposable valid `codex-test-*.zip` inside `openreward-001\www`, or complete the exact zip worker flow first, then run unzip against only that disposable archive. | Partially unblocked |
| 2026-06-12 | Apps | Full one-click plugin install execution | Needs `plugin_process_3_action.asp` ported end-to-end plus a disposable target site/database and reachable legacy file-manager/IIS/database agents before executing writes | Latest `/Users/erwinyu/Downloads/hosting/cp8/cp/Plugins/plugin_process_3_action.asp` runs `install_1` DB create, `install_2` SQL import, `install_3` folder create, `install_4` ZIP upload/unzip via `/newfileman/unzip_plugin.asp`, `install_5` config edits, and optional permission/IIS app steps from `fn_plugin.inc`. The rebuilt app now reads real requirement metadata from `plugins.dbo.parameters`, `plugins.dbo.config`, and `plugins.dbo.permissions`, validates site/database/path ownership through `POST /api/hosting/apps/install-preview`, and exposes a guarded `POST /api/hosting/apps/install` that blocks before writes. Smoke tests used live plugin `MyWebPagesStarterKit` and confirmed no-write planning plus rejection of `h:\root\home` and `..\..\outside` target paths. | Confirm a disposable `codex-test-*` website/database target after the legacy file-manager and DB agents are reachable; then port each `plugin_process_3_action.asp` step exactly before enabling writes. | Blocked |
| 2026-06-12 | Databases | Live delete test for MSSQL/MySQL disposable databases | Need a disposable `codex-test` database row before verifying remote delete and local row cleanup | Latest `/Users/erwinyu/Downloads/hosting/cp8/cp/mssql_action.asp` and `mysql_action.asp` delete additional users first, then call `fn_db.inc:delete_mssql/delete_mysql`, which posts `MSSQL_Member_DB_Delete` or `MySQL_Member_DB_Delete`, marks the DB row deleted, removes DB users, and clears `customDBBackup`. The rebuilt `DELETE /api/hosting/databases/{engine}/{id}` now implements this chain only for disposable database names; a smoke test against existing `DB_9E47E1_openreward` correctly returned a guarded failure before any write. | Create/confirm a disposable DB named with `codex-test`/`codex_test`, then run create/read/delete verification against only that row. | Blocked |
| 2026-06-12 | Databases | MSSQL Reporting Service user writes | Need `encryptpwd` compatibility plus Windows local-user RPC test target | Latest `/Users/erwinyu/Downloads/hosting/cp8/cp/mssqlreportusers_action.asp` creates/updates/deletes SSRS site users by calling `createWindowsLocalUser`, `updateWindowsLocalUser`, `deleteWindowsLocalUser`, and `AddSiteUserDB`/`DeleteSiteUserDB`. The rebuilt `GET /api/hosting/databases/mssql-report-users` now reads `cp_config_SSR` and `cp_config_site_users`; POST/PUT/DELETE routes return an explicit blocker before writing. Smoke read for `openreward-001` returned `enabled=false`, one site user (`agapeapm`), and no SSRS server id. | Provide/confirm disposable SSRS user target and exact password encryption compatibility for `cp_config_site_users.password`, then test Windows local-user RPC against SSRS server only for that disposable user. | Blocked |
